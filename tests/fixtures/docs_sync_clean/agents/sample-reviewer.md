---
name: sample-reviewer
description: A sample review agent that enforces AB-2.
---

This agent verifies adherence to AB-2 (an advisory rule). It is the
enforcer named in the RULES.md index, so the index ↔ enforcer link is
consistent.
